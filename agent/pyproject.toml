[project]
name = "aegis-agent"
version = "0.1.0"
description = "Agente local de Aegis: intercepta y decide sin que el contenido salga del equipo"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["aegis_agent*"]
