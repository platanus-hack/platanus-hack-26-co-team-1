# Nivel T2: el modelo local. Va aparte porque es opcional y pesa: arrastra torch.
# Sin esto el agente sigue protegiendo con T1, que es donde vive la certeza.
#
# Despues de instalarlo conviene precargar el modelo una vez, para que la
# descarga de HuggingFace no ocurra durante el primer envio de alguien:
#
#   python -c "from gliner import GLiNER; GLiNER.from_pretrained('urchade/gliner_multi-v2.1')"
gliner>=0.2.28
# mitmproxy fija h11<=0.14 y las dependencias de gliner traen una version mas
# nueva. Sin este pin, pip deja el entorno con un conflicto que rompe el proxy.
h11==0.14.0
httpcore<1.0.6
